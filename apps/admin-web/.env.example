# Admin API base — server-side calls from next-auth (login/refresh)
ADMIN_API_URL=http://localhost:3002

# Admin API base — browser-side calls from dashboard pages (must be NEXT_PUBLIC_*)
NEXT_PUBLIC_ADMIN_API_URL=http://localhost:3002

# NextAuth — admin-web's own origin (dev runs on :3003)
NEXTAUTH_URL=http://localhost:3003
NEXTAUTH_SECRET=
